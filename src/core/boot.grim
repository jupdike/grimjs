// Built-in casts so arithmetic works as expected
// and so we don't have to make N x M callable entries for Div, for Mul, etc.
Int :> Nat
Rat :> Int
Rat :> Nat

// some test definitions, of scalars, and functions
x := 41
y := x + 1

f(x, y) := x + y
